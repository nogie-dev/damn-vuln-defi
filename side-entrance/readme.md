# 풀이

- 풀은 플래시론을 준 뒤 `execute` 콜백을 호출하며, 상환 검증은 “풀 잔고가 원래대로인지”만 본다.
- `execute`에서 받은 ETH를 다시 `deposit`하면 잔고 검증을 통과한다. 이때 입금액은 `msg.sender`의 예금으로 기록된다.
- 플래시론 종료 후 공격자는 `withdraw`로 본인 예금을 전부 인출해 복구 주소로 보낼 수 있다.
