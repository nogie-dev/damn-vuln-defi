# 풀이
- claimRewards는 같은 토큰이 연속될 때 _setClaimed를 바로 호출하지 않고 bitsSet·amount만 누적해두었다가 마
    지막 청구에서 한 번만 _setClaimed를 실행한다.
- 이때 중복된 배치까지 누적되면 “이미 청구됐는지” 검증이 앞선 반복에서 건너뛰고, 마지막에 한 번만 비트를
    찍는다.
- 결과적으로 동일 배치를 여러 번 넣어도 중복 검증 없이 금액은 모두 합산되어 지급되고, 상태는 한 번만 갱신
    돼 추가 탈취가 가능하다.